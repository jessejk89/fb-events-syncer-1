Create a file named config.py with the following contents:

fb_token = "TOKEN_EXAMPLE_CONTENT"
wp_cookie = "WORDPRESS_COOKIE_EXAMPLE_CONTENT"

Fill in your own tokens. Goto the top of the sync.py file and check all settings are in order.

To execute the script, run 
>python sync.py